run:
    hello world:
        code: |-
            package main
            import (
                "fmt"
            )
            func main(){
                fmt.Println("Hello World");
            }